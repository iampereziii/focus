# `components/review/` — disposition list, correction pass, day split

Five jobs, in order — see `src/app/(app)/review/page.tsx`.

The app **states the fact**; the architect does the reading. No blocking, no
extra questions, no interpretation by the app. Reclassification is offered,
**never forced**.

⚠️ If this screen has to be cut down, **cut job 1 first** (stack disposal — the
newest), never the review itself.
